apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "sa-common.fullname" . }}-heartbeat
  labels:
    {{- include "sa-common.labels" . | nindent 4 }}
    app.kubernetes.io/component: heartbeat
spec:
  schedule: {{ required "heartbeat.schedule is required" .Values.heartbeat.schedule | quote }}
  timeZone: {{ .Values.heartbeat.timeZone | quote }}
  concurrencyPolicy: {{ .Values.heartbeat.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.heartbeat.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.heartbeat.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      backoffLimit: {{ .Values.heartbeat.backoffLimit }}
      activeDeadlineSeconds: {{ .Values.heartbeat.activeDeadlineSeconds }}
      template:
        metadata:
          labels:
            {{- include "sa-common.selectorLabels" . | nindent 12 }}
            app.kubernetes.io/component: heartbeat
        spec:
          restartPolicy: Never
          serviceAccountName: {{ include "sa-common.serviceAccountName" . }}
          automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          containers:
            - name: cron-heartbeat
              image: "{{ required "image.repository is required" .Values.image.repository }}:{{ default $.Chart.AppVersion .Values.image.tag }}"
              imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
              args: ["heartbeat"]
              securityContext:
                {{- toYaml .Values.containerSecurityContext | nindent 16 }}
              envFrom:
                - configMapRef:
                    name: {{ include "sa-common.fullname" . }}
                - secretRef:
                    name: {{ include "sa-common.fullname" . }}
              {{- if .Values.extraEnv }}
              env:
                {{- range .Values.extraEnv }}
                - name: {{ .name | quote }}
                  value: {{ .value | quote }}
                {{- end }}
              {{- end }}
              resources:
                {{- toYaml .Values.resources | nindent 16 }}
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: tmp
              emptyDir: {}
