apiVersion: v2
name: gateway
description: API Gateway (Express reverse proxy) — único punto de entrada a los microservicios
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: sa-common
    version: 0.1.0
    repository: "file://../sa-common"
