apiVersion: v2
name: auth-service
description: Authentication microservice (NestJS + Prisma + PostgreSQL) — issues/validates JWTs
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: sa-common
    version: 0.1.0
    repository: "file://../sa-common"
